<% noindex %>
<% nofollow %>

<section class="bg-gray-50">
  <div class="container flex items-center min-h-screen">
    <div class="max-w-xl">
      <h1 class="text-3xl font-extrabold tracking-tight text-gray-900 sm:text-4xl">
        <%= title 'Sorry, we could not find that page' %>
      </h1>
      <%= link_to 'Go back', :back, class: 'inline-block underline font-medium mt-4' %>
    </div>
  </div>
</section>